document.addEventListener('DOMContentLoaded', function () {
    var actionbtn = document.getElementById('qno');

    actionbtn.addEventListener('click', function () {
        window.close();
    });
});